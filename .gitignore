# Node.js / npm / pnpm / yarn
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.pnpm-debug.log
.pnpm-store/

# Next.js build / cache
.next/
.out/
.next/cache/
.swc/
.vercel/

# OpenNext.js / Cloudflare adapter specific (খুব গুরুত্বপূর্ণ!)
.open-next/
worker.js
worker.js.map

# Wrangler / Cloudflare local state & dev files
.wrangler/
.dev.vars
.wrangler/state/
wrangler.toml  # যদি secrets থাকে → ignore করো, অথবা wrangler.toml.example রাখো
.cloudflare/

# Drizzle ORM migrations / snapshots (যদি local generated হয়)
drizzle/  # ← migration files commit করা উচিত! (যদি production-এ apply করতে চাও)
# যদি drizzle folder-এ snapshots বা meta থাকে যা regenerate হয় → সেগুলো ignore করো, কিন্তু migration .sql commit করো

# Environment variables & secrets
.env
.env.local
.env.development
.env.test
.env.production
.env*.local

# Editor / OS files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# Logs & temp files
*.log
tmp/
temp/
coverage/
dist/

# TypeScript / build artifacts
*.tsbuildinfo
*.d.ts.map
*.js.map

# Optional: tests coverage
coverage/

# Better Auth related (যদি কোনো generated secret বা cache থাকে)
auth-secret.txt  # যদি generate করো